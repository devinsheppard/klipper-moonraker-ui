{
  "name": "klipper-moonraker-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build && npm run verify:dist",
    "preview": "vite preview",
    "verify:dist": "node scripts/verify-dist.mjs",
    "build:dist": "npm run licenses:report && npm run build",
    "licenses:report": "node scripts/generate-license-report.mjs"
  },
  "dependencies": {
    "three": "^0.181.1",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.2",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vue-tsc": "^3.1.5"
  }
}
